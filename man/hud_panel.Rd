% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{hud_panel}
\alias{hud_panel}
\title{Wrap a magick image in a dark semi-transparent panel border}
\usage{
hud_panel(
  img,
  padding = 20,
  panel_color = "#111111CC",
  border_color = NA,
  border_width = 2,
  ...
)
}
\arguments{
\item{img}{A \code{magick-image} object, typically from \code{\link[=render_hud]{render_hud()}}.}

\item{padding}{Number of pixels to add around all four sides of \code{img}
before drawing the panel frame. Default \code{20}.}

\item{panel_color}{Background colour of the panel as a CSS hex string
(including alpha channel). Default \code{"#111111CC"}.}

\item{border_color}{Colour of the border drawn around the panel edge.
Set to \code{NA} (default) to omit the border.}

\item{border_width}{Line width of the border in pixels. Defaults to \code{2}.}

\item{...}{Additional arguments (currently unused, but reserved for future
extensions).}
}
\value{
A \code{magick-image} with size \code{width + 2*padding} by
\code{height + 2*padding} relative to the input.
}
\description{
Adds a dark, rectangular background (the "panel") around a \code{magick-image},
with an optional colored-border. The result is a larger image with
the original content centred on the panel, ready for \code{\link[=warp_hud]{warp_hud()}} or
\code{\link[=composite_hud]{composite_hud()}}.
}
\examples{
\dontrun{
library(ggplot2)
p   <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
img <- render_hud(p, width = 480, height = 320, bg = "transparent")

# panel with a green border
panel <- hud_panel(img,
                   panel_color  = "#1B6B3ACC",
                   border_color = "#57FF8877",
                   border_width = 2)
}

}
