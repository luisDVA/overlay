% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carbon.R
\name{carbon_image}
\alias{carbon_image}
\title{Create an image of a code snippet with carbon.js via the carbonara API}
\usage{
carbon_image(x, lang = "r", theme = c("dark", "light"), timeout = 60, ...)
}
\arguments{
\item{x}{Character string with the code to render.}

\item{lang}{Programming language. Default \code{"r"}. See carbonara documentation
for supported languages.}

\item{theme}{Overall look. \code{"dark"} (default, uses one-dark) or \code{"light"} (uses one-light).}

\item{timeout}{Request timeout in seconds. Defaults to \code{60}.}

\item{...}{Additional carbonara API parameters. Common options include:
\itemize{
\item \code{backgroundColor}: background color (e.g., \code{"rgba(171,184,195,1)"})
\item \code{theme}: theme name (overrides theme preset, e.g., \code{"monokai"}, \code{"nord"})
\item \code{fontFamily}: font family (e.g., \code{"Hack"}, \code{"Fira Code"})
\item \code{fontSize}: font size (e.g., \code{"14px"})
\item \code{lineNumbers}: show line numbers (\code{TRUE} or \code{FALSE})
\item \code{windowControls}: show window controls (\code{TRUE} or \code{FALSE})
}}
}
\value{
A \code{magick-image} object.
}
\description{
Generates a styled code image using the carbonara community API
(https://github.com/petersolopov/carbonara), which provides programmatic
access to carbon.js styling without a headless browser.
}
\examples{
\dontrun{
# Dark theme (default)
img <- carbon_image("x <- 1:10\nplot(x)")

# Light theme
img <- carbon_image("x <- 1:10\nplot(x)", theme = "light")

# Custom theme and settings
img <- carbon_image("x <- 1:10", theme = "dark", 
                    backgroundColor = "rgba(0,0,0,1)",
                    fontSize = "16px")
}

}
