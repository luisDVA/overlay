% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warp.R
\name{warp_hud}
\alias{warp_hud}
\title{Apply a perspective warp to a magick image}
\usage{
warp_hud(img, corners = list(), keep_size = TRUE, ...)
}
\arguments{
\item{img}{A \code{magick-image} object, typically produced by \code{\link[=render_hud]{render_hud()}}.}

\item{corners}{A named list specifying how far each corner of the image
should move from its natural position. Each element is a numeric vector
of length 2: \code{c(dx, dy)}, where positive values move right/down.
Names must be \code{"tl"} (top-left), \code{"tr"} (top-right), \code{"bl"}
(bottom-left), and \code{"br"} (bottom-right). Any corner omitted defaults
to \code{c(0, 0)} (no movement).

\strong{Example}: tilt the panel so the left edge rises and the right
edge drops:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{corners = list(tl = c(0, -20), bl = c(0, -20),
               tr = c(0,  20), br = c(0,  20))
}\if{html}{\out{</div>}}}

\item{keep_size}{If \code{TRUE} (default), the output image is cropped/padded
back to the original pixel dimensions. If \code{FALSE}, ImageMagick may
expand the canvas to contain the warped result.}

\item{...}{Currently unused.}
}
\value{
A warped \code{magick-image} with an alpha channel preserved.
}
\description{
Distorts a \code{magick-image} using a four-point perspective transformation,
giving the "floating HUD panel" effect.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
img <- render_hud(p, width = 500, height = 350)

# Tilt the top edge backward (raise both top corners)
warped <- warp_hud(img, corners = list(tl = c(20, -15), tr = c(-20, -15)))
magick::image_browse(warped)
}

}
